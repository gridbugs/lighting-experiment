imports = [
    'cgmath::Vector2',
    'content::DoorState',
]

[components]
coord = { type = 'Vector2<i32>' }
position = { type = 'Vector2<f32>' }
solid = {}
opacity = { type = 'f64' }
door_state = { type = 'DoorState' }
